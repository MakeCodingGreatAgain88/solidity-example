{
  "name": "example-hardhat",
  "version": "1.0.0",
  "description": "example-hardhat",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "hardhat run scripts/deployFundMe.ts",
    "deploy:sepolia": "npx env-enc set-pw && hardhat run scripts/deployFundMe.ts --network sepolia",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@chainlink/contracts": "^1.4.0",
    "@chainlink/env-enc": "^1.0.5",
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^17.2.0",
    "hardhat": "^2.26.0"
  }
}
